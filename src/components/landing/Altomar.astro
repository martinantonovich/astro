<header class="bg-zinc-900 flex flex-col md:flex-row p-20 gap-3 text-center md:text-left rounded-xl relative z-10">
    <div>
        <h1 class="text-5xl font-bold pb-2">El desarrollo de software al alcance de tu mano</h1>
    
        <p class="my-4 text-2xl">
            Altomar es una empresa dedicada al desarrollo de software tanto de front-end como de back-end. 
            Nos dedicamos al desarrollo combinando nuestro conocimiento, nuestra experiencia y la tecnología para brindar soluciones personalizadas a tus necesidades.
        </p>
    
        <button
            onclick="openModal()"
            class="bg-indigo-500 px-4 py-2 rounded hover:bg-indigo-400"
        >
            Contactarse
        </button>
        </div>
    
        <img src="/astro/images/ia.jpg" alt="" class="w-80 mx-auto md:mx-0" />
    </header>
    
    <!-- Modal -->
    <div
        id="contact-modal"
        class="fixed inset-0 bg-black/50 hidden justify-center items-center z-50"
        onclick="closeModal()"
    >
        <div
        class="bg-white text-black rounded-lg shadow-lg p-6 w-full max-w-md relative"
        onclick="handleModalClick(event)"
        >
        <!-- Cerrar -->
        <button
            class="absolute top-2 right-2 text-gray-500 hover:text-gray-800"
            onclick="closeModal()"
        >
            ✖
        </button>
    
        <h2 class="text-2xl font-bold mb-4">Contacto</h2>
    
        <form class="flex flex-col gap-4">
            <input
            type="text"
            placeholder="Tu nombre"
            class="border rounded px-3 py-2"
            />
            <input
            type="email"
            placeholder="Tu correo"
            class="border rounded px-3 py-2"
            />
            <textarea
            placeholder="Tu mensaje"
            class="border rounded px-3 py-2"
            rows="4"
            ></textarea>
            <button
            type="submit"
            class="bg-indigo-500 text-white px-4 py-2 rounded hover:bg-indigo-400"
            >
            Enviar
            </button>
        </form>
        </div>
    </div>
    
    <!-- Script (importante que esté después del HTML) -->
    <script is:inline>
        function openModal() {
        document.getElementById('contact-modal').classList.remove('hidden');
        document.getElementById('contact-modal').classList.add('flex');
        }
    
        function closeModal() {
        document.getElementById('contact-modal').classList.remove('flex');
        document.getElementById('contact-modal').classList.add('hidden');
        }
    
        function handleModalClick(e) {
        e.stopPropagation(); // evita que se cierre el modal al hacer clic dentro
        }
</script>
